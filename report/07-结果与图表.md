# 7. 结果与图表

## G4 结果

`docs/04-数据与图表/memory-model-matrix.csv` 共 20 行：15 行 `real`，5 行 `real-threshold`；20 行状态均为 `pass`，`observed_mb` 全填，`error_pct=0.0000`。旧公式失效案例包括 `7147 MB`、`17459 MB` 等真实后端报错，原始日志包含建表、插入、NOTICE、ERROR 的完整前置序列。

## G10 结果

零参数审计日志显示：1000 行表、`vector(3)`、IVFFlat `lists=10`，推荐 lists 为 1、recommended probes 为 4；HNSW 行不填 IVFFlat 专属列。函数定义和 `pg_proc.pronargs` 均证明参数个数为 0。

## 已生成图

1. `report/figures/output/01_formula_log.svg`：20 组新旧公式与 observed 对数轴对照，源数据为 `report/figures/data/memory_model_matrix.csv`。
2. `report/figures/output/02_error_matrix.svg`：20 组绝对相对误差，源数据为同一矩阵快照。
3. `report/figures/output/03_audit_table.svg`：零参数审计实测输出，源数据为 `report/figures/data/audit-zero-param-full.tsv`，包含 predicted build memory 和 `maintenance_work_mem`。

三张图均由对应脚本生成；large 场景仍按 `real-threshold` 限制解释。
