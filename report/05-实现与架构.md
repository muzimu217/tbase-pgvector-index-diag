# 5. 实现与架构

实现包括源代码推导的 `sql/02_memory_model.sql`、目录快照 `sql/04_catalog_introspect.sql`、零参数审计 `sql/05_audit.sql` 和验证 harness `tools/validate_memory_model.sh`。审计输出将 IVFFlat 专属 lists/probes 与 HNSW 通用信息区分；`possible_seqscan` 只依据静态目录风险，明确不替代 EXPLAIN。

