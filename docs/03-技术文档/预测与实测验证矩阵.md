# IVFFlat 预测与实测验证矩阵

> 生成命令：`MODE=real tools/validate_memory_model.sh`。

## 当前状态

20 组配置；20 组通过，0 组失败，0 组尚未取得后端报错原文。

**G4: formally passed (R009, 2026-08-17 PDT).** All 20 predictions have captured pgvector error values and absolute error below 5%; the final decision is recorded in the supervision archive.

## 分层方法

- `small`/`saturated`：按 CSV 的 `sample_rows` 插入数据并创建实际 IVFFlat 索引，`maintenance_work_mem` 设为预测值以下，捕获 pgvector 的 `memory required is X MB`。
- `large`：模型仍按 CSV 的 `rows`、维度和 lists 计算源码目标容量；后端实际插入 `sample_rows=min(rows, max(lists*50, 10000))`，再把 `maintenance_work_mem` 设为预测值减 1 MB。pgvector 在分配巨型数组前返回同一原始错误，记录为 `real-threshold`，避免 OOM；这验证检查点和模型口径，不等同于完成巨型索引构建。
- 误差定义：`abs(observed - predicted) / predicted * 100`；`<5%` 才计为通过。

## 必含旧公式失效场景

| 场景 | 新模型 MB | 旧公式 MB |
|---|---:|---:|
| 10000000 / 128d / lists=100 | 10 | 19537 |
| 1000000 / 1536d / lists=1000 | 506 | 23494 |
| 200000 / 384d / lists=8192 | 7147 | 1594 |
| 100000 / 128d / lists=32768 | 17459 | 1850 |

> 口径核对：按当前仓库的 `ivfkmeans.c:277-299` 和 `VECTOR_ARRAY_SIZE` 逐项计算，100k/128d/lists=32768 为 17,459 MB；`old-fail-04` 原始后端错误同样报告 17,459 MB。监督清单中曾写约 16,695 MB，已由源码推导和实测共同排除，不能为了贴合预期值修改源码推导。

## 执行环境

- `authorized dev-server (address redacted); OpenTenBase 5.0 commit 0915c04e4; pgvector 0.8.0; Coordinator 22201; x86_64; 3.7 GiB RAM + 2 GiB swap`

## 结果文件

- CSV：`docs/04-数据与图表/memory-model-matrix.csv`
- 原始后端日志目录：`raw-memory-errors`
